public struct ExplodeGemsComponent
{
    public float delay;
    public float delayCounter;

    public ExplodeGemsComponent(float delay)
    {
        this.delay = delay;
        delayCounter = 0f;
    }
}
